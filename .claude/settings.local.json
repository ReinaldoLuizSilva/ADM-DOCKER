{
  "permissions": {
    "allow": [
      "Bash(pip install *)",
      "PowerShell(pip install docker python-dotenv)",
      "PowerShell(python --version)",
      "PowerShell(python -m pip install docker python-dotenv)",
      "PowerShell(python -m pip install paramiko)"
    ]
  }
}
